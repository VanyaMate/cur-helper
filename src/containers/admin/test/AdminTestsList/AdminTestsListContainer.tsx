import React from 'react';


export type AdminTestsListContainerProps = {};

const AdminTestsListContainer: React.FC<AdminTestsListContainerProps> = (props) => {
    const {} = props;

    return (
        <div>
            // list
        </div>
    );
};

export default React.memo(AdminTestsListContainer);