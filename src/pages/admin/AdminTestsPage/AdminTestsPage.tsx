import React from 'react';


export type AdminTestsPageProps = {};

const AdminTestsPage: React.FC<AdminTestsPageProps> = (props) => {
    const {} = props;

    return (
        <div>
            AdminTestsPageComponent
        </div>
    );
};

export default React.memo(AdminTestsPage);